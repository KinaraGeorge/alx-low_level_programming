#include <stdio.h>
/**
 * main - prints phrase using printf
 * Return: executed if runs as expected
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
