#include <stdio.h>

/**
 * main - prints all base 10 number starting from 0
 * Return: 0 if successful
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
